Q16 — Kafka + Idempotency

A producer sends:

transaction_id = T100

to Kafka.

Due to a producer retry, the same logical transaction is written twice:

T100 → partition 3, offset 101
T100 → partition 3, offset 102

Which statement is correct?

A. Kafka automatically removes the duplicate because the key is the same.

B. Kafka guarantees that only one message with the same key exists.

C. The consumer/application must handle the possibility of duplicate logical events unless appropriate producer/consumer semantics and application design prevent the business impact.

D. Spark automatically removes all Kafka duplicates.

Follow-up: Why is transaction_id being the Kafka key not equivalent to deduplication?

Q17 — Watermark + Aggregation

You calculate:


total sales by customer and event-time window

using Structured Streaming.

Events can arrive up to 20 minutes late.

You configure:

watermark = 10 minutes

The business says:

"We cannot lose valid transactions just because they arrived late."

What would you discuss with the business before simply changing the watermark to 20 or 30 minutes?

Give me at least 4 considerations.

Q18 — Delta MERGE

You receive CDC data:

customer_id | operation | updated_at
------------|-----------|------------
101         | UPDATE    | 10:05
101         | UPDATE    | 10:03

The records arrive out of order.

If you blindly execute:

MERGE source INTO target

what problem could occur?

A. The older update could overwrite the newer state.

B. Delta automatically sorts CDC records by updated_at.

C. Kafka guarantees that CDC records arrive in timestamp order.

D. Spark automatically chooses the latest record.

How would you make the pipeline safer?

Q19 — Spark Small Files

Your streaming pipeline writes to Delta every 30 seconds.

After several months you discover:

Millions of tiny files
Slow queries
Slow metadata operations
High storage overhead

What is the likely cause?

A. Too many small streaming writes/files.

B. Kafka partitions are too large.

C. Watermark is too short.

D. Delta cannot store streaming data.

What production strategies would you consider to address the problem?

Q20 — Lead Architecture Decision

Your team proposes:

Kafka → Spark → Delta Bronze → Delta Silver → Delta Gold

Another architect proposes:

Kafka → Spark → Data Warehouse

The team asks you:

"Which architecture is better?"

As the Lead Engineer, would you answer A or B, or would you reject the framing?

Explain your decision-making approach.