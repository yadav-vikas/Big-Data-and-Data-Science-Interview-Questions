Q1 — Databricks / Delta Lake

You have a Databricks pipeline processing 2 TB of daily event data. The source data arrives as Parquet files in cloud storage.

The pipeline currently does:

Raw Parquet → Spark transformation → overwrite entire target Parquet table

The business now wants:

incremental processing
ability to handle late-arriving records
ACID transactions
historical versioning/time travel
efficient updates to existing records

Which design is most appropriate?

A. Continue using Parquet but partition by date and overwrite only the affected partitions.

B. Convert the target into a Delta table and use MERGE for incremental upserts.

C. Store the data in Kafka and use Kafka compaction to maintain the latest version.

D. Use a traditional data warehouse table and perform a full reload every day.

Q2 — PySpark Performance

You have this PySpark transformation:

df.groupBy("customer_id").count()

The input contains 5 billion records, and customer_id has a highly skewed distribution: one customer represents 30% of all records.

The job is extremely slow because one Spark task runs much longer than the others.

What is the primary issue?

A. Insufficient executor memory

B. Data skew causing an uneven distribution of shuffle data

C. Spark cannot perform aggregation on billions of rows

D. The DataFrame needs to be converted into an RDD

And briefly explain how you would fix it in a production pipeline.

Q3 — Kafka Architecture

You have a Kafka topic:

customer_events

with 12 partitions.

There are 3 consumer instances belonging to the same consumer group.

How will Kafka distribute consumption?

A. Every consumer receives every message from all 12 partitions.

B. Each partition is consumed by exactly one consumer within the consumer group, so partitions are distributed across the 3 consumers.

C. Kafka automatically creates 36 partitions, one for each consumer-partition combination.

D. Each consumer receives messages randomly regardless of partition assignment.

Now the follow-up:

What happens if you increase the consumer instances from 3 → 15 while keeping the topic at 12 partitions?

Q4 — Spark Structured Streaming

You have a streaming job reading transactions from Kafka:

Kafka → Spark Structured Streaming → Delta Lake

The same transaction can occasionally appear twice in Kafka because of producer retries.

You need the Delta table to contain one logical record per transaction_id.

Which approach is most appropriate?

A. Use dropDuplicates("transaction_id") with an appropriate watermark.

B. Use repartition("transaction_id") and assume duplicates disappear.

C. Use cache() before writing to Delta.

D. Increase Spark executor memory.

Explain why the watermark matters rather than simply saying "it removes duplicates."

Q5 — Data Warehouse Design

A company has an orders dataset containing:

order_id
customer_id
product_id
order_date
quantity
unit_price
discount
shipping_cost

The analytics team frequently asks:

"What was the total revenue by customer, product, region, and month?"

You are designing a dimensional data warehouse.

Which design is the best starting point?

A. One highly normalized table containing all order information.

B. fact_order containing measurable/order-level facts, surrounded by dimensions such as dim_customer, dim_product, dim_date, and potentially dim_region.

C. Separate fact tables for every possible analytical question.

D. Store everything as JSON in a single table to make future requirements easier.