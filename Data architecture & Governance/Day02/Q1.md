Q6 — Spark Join + Data Skew

You have:

orders = 800 GB
customers = 5 GB

You need:

orders.join(customers, "customer_id")

The customers table fits comfortably within executor memory.

Which approach would usually be preferable?

A. Broadcast the customers DataFrame.

B. Broadcast the orders DataFrame.

C. Convert both DataFrames to RDDs.

D. Always repartition both datasets to the same number of partitions.

Follow-up: What risks would you consider before using a broadcast join in production?

Q7 — Kafka Ordering

You have a Kafka topic with 10 partitions.

The business requirement says:

"Events for the same customer must always be processed in order."

What is the best producer-side strategy?

A. Send all events to partition 0.

B. Use customer_id as the Kafka message key.

C. Randomly distribute events across partitions.

D. Increase the topic to 100 partitions.

Explain what ordering Kafka actually guarantees.

Q8 — Structured Streaming Checkpointing

Your pipeline is:

Kafka
  ↓
Spark Structured Streaming
  ↓
Delta Lake

The Spark application crashes after processing some Kafka records but before the application successfully completes the corresponding micro-batch.

When the application restarts with the same checkpoint location, what is the primary purpose of the checkpoint?

A. It stores all source Kafka data permanently.

B. It allows Spark to recover streaming progress/state and continue from the appropriate point.

C. It guarantees that every external sink is exactly-once regardless of implementation.

D. It eliminates the need for Kafka offsets.

Follow-up: Why should you generally not delete or randomly change the checkpoint location during a production deployment?

Q9 — Delta Lake / Concurrent Writes

Two independent jobs simultaneously update the same Delta table:

Job A → UPDATE customer_id = 100
Job B → UPDATE customer_id = 100

Both jobs read the table before either commits.

What mechanism is Delta Lake primarily using to maintain transactional consistency?

A. Traditional database row-level locking for the entire operation.

B. Optimistic concurrency control with transaction validation at commit time.

C. Kafka consumer offsets.

D. Spark caching.

Follow-up: What can happen to one of the jobs if Delta detects a conflicting concurrent modification?

Q10 — Streaming Architecture Design

You need to build a real-time fraud detection platform.

Requirements:

transactions arrive continuously
fraud rules need to be evaluated within seconds
analysts need historical reporting
data scientists need raw and historical data
events can arrive late
duplicate events can occur
the system must tolerate application failures

You propose:

Payment API
    ↓
   Kafka
    ↓
Spark Structured Streaming
    ↓
Delta Lake
    ↓
Data Warehouse

Would you consider this architecture reasonable?

A. Yes, this is a reasonable high-level architecture, but several concerns need to be addressed.

B. No, Kafka should be removed because Delta Lake can replace it.

C. No, Spark Structured Streaming cannot process Kafka data.

D. No, data warehouses cannot consume analytical data.

If you choose A, identify at least 5 production concerns/design decisions you would discuss before approving the architecture.