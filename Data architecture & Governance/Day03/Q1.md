Q11 — Kafka + Spark: Duplicate Processing

A payment event has:

transaction_id = T100
amount = 500

Spark reads it from Kafka and successfully writes it to Delta.

Before Spark's progress/checkpoint is safely committed, the application crashes.

After restart, Spark may read T100 again.

What is the best architectural approach to ensure the Delta layer doesn't end up with two copies?

A. Increase Kafka partitions.

B. Use a deterministic transaction/event identifier and design the Delta write for idempotency/deduplication.

C. Increase the Spark trigger interval.

D. Use cache() before writing to Delta.

Follow-up: Explain the difference between "Spark processed the record twice" and "Delta contains the record twice."

Q12 — Spark Partitioning

You have a Delta table containing 3 TB of data.

It is partitioned by:

country

There are only 5 countries.

A query frequently runs:

SELECT *
FROM sales
WHERE customer_id = 12345

The query is slow.

A developer says:

"Let's partition the table by customer_id because it will make this query fast."

Would you approve this?

A. Yes, always partition on the most frequently filtered column.

B. No. High-cardinality partitioning can create too many small files/directories; consider other optimization techniques such as data skipping/clustering depending on the platform/version and workload.

C. Yes, because customer_id uniquely identifies every record.

D. No, because partitioning has no impact on query performance.

Explain your reasoning.

Q13 — Structured Streaming Watermark

You receive events with:

event_time

Typical delay is 2 minutes, but occasionally events arrive 15 minutes late.

You configure:

.withWatermark("event_time", "10 minutes")

An event generated at 10:00 arrives at 10:16.

What is the important question you need to ask before saying:

"Spark will definitely discard this event."

A. Whether the event arrived through Kafka.

B. What the current watermark/event-time progress is and how the query's stateful operation handles late data.

C. Whether the Spark cluster has enough CPU.

D. Whether the event is stored as JSON.

This is testing whether you understand that a watermark is not simply "arrival time minus 10 minutes."

Q14 — Data Warehouse: Slowly Changing Dimensions

Your customer dimension contains:

customer_id = 101
customer_name = "ABC Ltd"
region = "North"

On August 26, the customer moves to the South region.

The business says:

"Historical reports must continue showing the customer as North for orders placed before August 26, while new orders should show South."

Which approach is most appropriate?

A. Overwrite region in the existing customer row.

B. Implement an appropriate SCD Type 2 pattern with effective dates/current-record indicators.

C. Delete the customer and recreate it.

D. Store region only in the order fact table.

Explain why Type 1 is insufficient here.

Q15 — End-to-End Architecture

Your streaming pipeline currently has:

Kafka
 ↓
Spark
 ↓
Delta
 ↓
BI

Business reports show that yesterday's revenue was:

₹10.2 crore

but today the same historical day shows:

₹10.7 crore

The business asks:

"Why did historical revenue change?"

As the Lead Data Engineer, what would you investigate first?

Give me a structured investigation approach rather than simply saying "check the pipeline."